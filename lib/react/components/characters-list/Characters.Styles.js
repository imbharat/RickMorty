const charactersList = {
  display: "flex",
  flexWrap: "wrap",
}

const character = {
  height: "17rem",
  textAlign: "left",
  display: "flex",
  backgroundColor: "rgb(60, 62, 68)",
  margin: "1rem",
  flex: "1 0 32%",
  color: "white",
  fontFamily: `-apple-system, "BlinkMacSystemFont", "Segoe UI", "Roboto",
    "Helvetica", "Arial", sans-serif, "Apple Color Emoji", "Segoe UI Emoji",
    "Segoe UI Symbol"`,
  borderRadius: "1rem",
}

const characterImg = {
  borderRadius: "1rem 0 0 1rem",
  height: "17rem",
  width: "17rem",
}

const details = {
  margin: "1rem",
  width: "100%"
}

const section = {
  marginBottom: "1rem",
}

const characterName = {
  margin: "0",
  fontSize: "1.3rem",
  fontWeight: "800",
  fontStyle: "italic",
  color: "#ee7777",
}

const characterStatus = {
  fontSize: "1rem",
  fontWeight: "600",
}

const characterLocations = {
  fontSize: "0.8rem",
}

const characterLocationsHead = {
  fontSize: "1rem",
  color: "#ee7777"
}

export default {
  charactersList,
  character,
  characterImg,
  details,
  section,
  characterName,
  characterStatus,
  characterLocations,
  characterLocationsHead
}
